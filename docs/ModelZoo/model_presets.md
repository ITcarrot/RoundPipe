# Model Presets / 预设模型

本页列出了`wrap_model_to_roundpipe`可以识别并自动转换为`RoundPipe`的模型类。

This page lists the model classes that `wrap_model_to_roundpipe` can recognize and automatically convert to `RoundPipe`.

## Transformers

| Model Class / 类名 | Model Family / 模型系列 |
|---|---|
| `GptOssForCausalLM` | GPT-OSS |
| `LlamaForCausalLM` | Llama 3.x, Llama 3, Llama 2, LLaMA, Yi |
| `Qwen3ForCausalLM` | Qwen3 |
| `Qwen3MoeForCausalLM` | Qwen3 MOE |
